services:
  - type: web
    name: contamination-gauge-api
    env: docker
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: API_KEYS
        sync: false
    healthCheckPath: /health
    plan: free